version: '3.8'

services:
    api:
        build: .
        ports:
            - "8080:8080"
        depends_on:
            - db
        environment:
            - ConnectionStrings__RecipeDb=Host=db;Database=recipedb;Username=recipeuser;Password=P@ssW0rd!
    
    db:
        image: postgres:15-alpine
        environment:
            - POSTGRES_USER=recipeuser
            - POSTGRES_PASSWORD=P@ssW0rd!
            - POSTGRES_DB=recipedb 
        ports:
            - "5432:5432"
        volumes:
            - recipe-data:/var/lib/postgresql/data
volumes:
    recipe-data:
 